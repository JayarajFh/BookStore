name: Catalog Service

on:
  push:
    paths:
      - catalog-service/**
    branches:
      - main
  pull_request:
    branches:
      - main

jobs:
  build:
    name: Build and Push Catalog Service
    runs-on: ubuntu-latest

    defaults:
      run:
        working-directory: ./catalog-service

    steps:
      # 1️⃣ Checkout source code
      - name: Checkout code
        uses: actions/checkout@v5

      # 2️⃣ Setup Java 21
      - name: Setup Java 21
        uses: actions/setup-java@v5
        with:
          distribution: temurin
          java-version: 21
          cache: maven

      # 3️⃣ Fix mvnw permissions (Linux runner requirement)
      - name: Make Maven Wrapper executable
        run: chmod +x mvnw

      # 4️⃣ Build project (skip tests in CI)
      - name: Build with Maven
        run: ./mvnw -ntp verify -DskipTests

      # 5️⃣ Login to Docker Hub (ONLY on main branch)
      - name: Login to Docker Hub
        if: github.ref == 'refs/heads/main'
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}

      # 6️⃣ Build Docker image + Push (EXACT SAME NAME)
      - name: Build and Push Docker Image
        if: github.ref == 'refs/heads/main'
        run: |
          IMAGE=docker.io/${{ secrets.DOCKERHUB_USERNAME }}/bookstore-catalog-service:latest

          echo "Building image: $IMAGE"
          ./mvnw spring-boot:build-image \
            -DskipTests \
            -Dspring-boot.build-image.imageName=$IMAGE

          echo "Verifying image exists locally"
          docker images | grep bookstore-catalog-service

          echo "Pushing image: $IMAGE"
          docker push $IMAGE
